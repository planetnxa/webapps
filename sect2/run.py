from app import app
if __name__=="__main__":
    app.run(debug=True)
# still confused, app like the folder or the actual app
# so i need to be in sect
# but also, i had to use python run.py because flask did not work
# and what does adding to bash mean
# and why does flask run not work......